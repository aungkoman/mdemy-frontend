import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['drtoken.live', 'drtoken.live/storage','cdn.hellosayarwon.com','yt3.googleusercontent.com','vecardigitalprogramming.com','sellbery.com'],
  },
};

export default nextConfig;
